{
  "entities": {
    "Comment": {
      "title": "Comment",
      "description": "A comment on a shipment timeline node.",
      "type": "object",
      "properties": {
        "userId": {
          "type": "string",
          "description": "The unique ID of the author (Firebase UID)."
        },
        "authorName": {
          "type": "string",
          "description": "The name of the author."
        },
        "message": {
          "type": "string",
          "description": "The content of the comment."
        },
        "createdAt": {
          "type": "string",
          "format": "date-time",
          "description": "The timestamp when the comment was created."
        }
      },
      "required": ["userId", "authorName", "message", "createdAt"]
    }
  },
  "auth": {
    "providers": ["anonymous"]
  },
  "firestore": {
    "/shipments/{shipmentId}/shipment_nodes/{nodeId}/comments/{commentId}": {
      "schema": {
        "$ref": "#/backend/entities/Comment"
      },
      "description": "Stores comments for a specific node within a shipment's timeline."
    }
  }
}
